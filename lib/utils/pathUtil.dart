import 'package:flutter_cache_manager/flutter_cache_manager.dart';

class PathUtil{
  // static Future<String> findPath(String imageUrl) async {
  //   final cache = await CacheManager();
  //   final file = await cache.getFile(imageUrl);
  //   return file.path;
  // }
}